spring:
  application:
    name: bus-ticket-booking
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/busticketbooking
    username: root
    password: ABCDEFGH
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
